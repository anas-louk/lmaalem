/// Enums globaux de l'application

/// État de chargement
enum LoadingState {
  idle,
  loading,
  success,
  error,
}

/// Type de notification
enum NotificationType {
  info,
  success,
  warning,
  error,
}

/// Taille d'écran
enum ScreenSize {
  mobile,
  tablet,
  desktop,
}

